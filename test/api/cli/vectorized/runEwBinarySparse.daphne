// same seed ensures same placement of elements (due to random implementation)
X = rand(10000, 1000, 0.5, 1.0, 0.04, 7);
Y = rand(10000, 1000, 0.0, 0.5, 0.04, 7);

// Arithmetic
print(sum(X + Y));
print(sum(X * Y));
// TODO: left arithmetic ops

// Min/max
// TODO: print(sum(min(X, Y)));
// TODO: print(sum(max(X, Y)));

// Logical
// TODO: and, or, xor

// TODO: string ops (concat)

// TODO: sparse Comparisons
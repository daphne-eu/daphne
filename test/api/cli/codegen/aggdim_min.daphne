// Compare precompiled kernel with codegen generated for the *AggMinOp.

// Row
X = [1.0, 2, 3, 4, 5, 6](2, 3);
a = aggMin(X, 0);
print(a);

a = aggMin(as.matrix<si64>(X), 0);
print(a);

a = aggMin(as.matrix<ui64>(X), 0);
print(a);


// Column
X = [1.0, 2, 3, 4, 5, 6](2, 3);
a = aggMin(X, 1);
print(a);

a = aggMin(as.matrix<si64>(X), 1);
print(a);

a = aggMin(as.matrix<ui64>(X), 1);
print(a);
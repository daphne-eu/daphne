// Check that our inference doesn't throw errors if two SCF paths lead to different sparsity

G = rand(1, 1, 1.0, 1.0, 0.5, -1);
if (sum(G) > 0.5) {
    G = rand(100, 100, 1.0, 1.0, 0.001, -1);
}
else {
    G = rand(100, 100, 1.0, 1.0, 1.0, -1);
}
print(G);
# Test reading from a file when the file path is not trivially constant (i.e., a parameter to a UDF)
def readFrameFromCSV(path: str){
    print(readFrame(path));
}

print(readFrame("test/api/cli/io/ref/ReadCsv1-1.csv"));
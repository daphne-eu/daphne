fun fib(n : si64) -> si64 {
    res = 1;
    // TODO: return in if clause (when compiler supports it)
    if (n <= 0)
        res = 0;
    if (n > 2)
        res = fib(n - 1) + fib(n - 2);
    return res;
}

for(i in 0:7)
    print(fib(i));

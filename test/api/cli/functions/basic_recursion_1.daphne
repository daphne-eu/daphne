fun fib(n : si64) -> si64 {
    res = 1;
    // TODO: return in if clause (when compiler supports it)
    if (n > 1)
        res = fib(n - 1) + fib(n - 2);
    return res;
}

for(i in 1:5)
    print(fib(i));

# Focus on type inference.
## ColAgg # TODO We don't have kernels for this yet.
#print(idxMin(reshape([3, 4, 4, 3], 2, 2), 1));
#print(idxMin(reshape([3.3, 4.4, 4.4, 3.3], 2, 2), 1));
#print("");
# RowAgg
print(idxMin(reshape([3, 4, 4, 3], 2, 2), 0));
print(idxMin(reshape([3.3, 4.4, 4.4, 3.3], 2, 2), 0));
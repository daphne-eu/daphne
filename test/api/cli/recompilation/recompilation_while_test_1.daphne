X = rand(10, 10, 0, 10, 0.8, 42);
Y = rand(10, 10, 0, 10, 0.8, 42);

Z = X + Y + 1e-6;
P = (X + Y)*2 + 1e-6;
k = 10;
c = 0;

while (c <= 10) {
    Z = Z + log(Z, k);
    P = P + log(P, k);
    c = c + 1;
}

print(sum(Z));
print(sum(P));
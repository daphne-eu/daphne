// using single precision floating point values (fp32)
sizeOfValueType=4;
size=$numRows * $numCols * sizeOfValueType;
sizeInMB=size / 1024^2;
print("Generating ", 0); print(sizeInMB, 0); print("MB of random data");
XY = rand($numRows, $numCols, as.f32(0.0), as.f32(1.0), 1, -1);
print("Writing to file ", 0); print($filename);
write(XY, $filename);